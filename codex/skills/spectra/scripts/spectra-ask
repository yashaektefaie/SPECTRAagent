#!/usr/bin/env bash
set -euo pipefail

export TMPDIR="${TMPDIR:-/ewsc/${USER}/tmp}"
mkdir -p "$TMPDIR"

if command -v micromamba >/dev/null 2>&1; then
  exec micromamba run -n pgt spectra ask "$@"
fi

exec spectra ask "$@"
